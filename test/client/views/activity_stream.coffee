define () ->
	describe 'Activity Stream View', ()->	
		describe 'Aggregating input activities', ()->
			it 'should aggregate several edits on the same wiki page by the same person'
			
		describe 'Publisher interaction', ()->
			it 'should display prepend the new published post'
			it 'should display prepend the activity about the new published wiki page'
