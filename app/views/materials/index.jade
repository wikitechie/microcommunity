.row-fluid
	.span12
		if server.currentUser && server.currentUser.role == 'teacher'
			.pull-right	
				a.btn.btn-primary(href='/materials/new') Add a new material	
		h1.page-header
			= server.data.queryTitle 
			
.row-fluid
	.span6	
		#materials-region
	.span6
		#browse-materials-region
